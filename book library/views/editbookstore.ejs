<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Edit Book</title>

<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap');

:root{
  --bg:#f8fafc;
  --card:#ffffff;
  --primary:#6366f1;
  --text:#0f172a;
  --muted:#64748b;
  --border:#e5e7eb;
}

*{box-sizing:border-box;}

body{
  margin:0;
  font-family:'Inter',sans-serif;
  background:linear-gradient(120deg,#eef2ff,#f8fafc);
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:40px;
  color:var(--text);
}

/* ---------- LAYOUT ---------- */
.editor{
  width:100%;
  max-width:1000px;
  background:var(--card);
  border-radius:28px;
  display:grid;
  grid-template-columns:360px 1fr;
  overflow:hidden;
  box-shadow:0 40px 100px rgba(0,0,0,.12);
}

/* ---------- PREVIEW ---------- */
.preview{
  background:linear-gradient(160deg,#1e293b,#020617);
  padding:30px;
  color:white;
  display:flex;
  flex-direction:column;
  justify-content:center;
  text-align:center;
}

.preview img{
  width:200px;
  height:280px;
  object-fit:cover;
  border-radius:20px;
  margin:auto;
  box-shadow:0 30px 80px rgba(0,0,0,.6);
}

.preview h2{
  margin:25px 0 6px;
  font-size:22px;
}

.preview p{
  font-size:14px;
  opacity:.75;
}

/* ---------- FORM ---------- */
.form-area{
  padding:45px;
}

.form-area h1{
  font-size:28px;
  margin-bottom:10px;
}

.form-area span{
  font-size:14px;
  color:var(--muted);
}

.form{
  margin-top:30px;
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:18px;
}

.full{grid-column:1/-1;}

label{
  font-size:12px;
  font-weight:600;
  color:var(--muted);
}

input,select{
  width:100%;
  margin-top:6px;
  padding:13px 14px;
  border-radius:12px;
  border:1px solid var(--border);
  font-size:14px;
  outline:none;
}

input:focus,select:focus{
  border-color:var(--primary);
  box-shadow:0 0 0 3px rgba(99,102,241,.15);
}

/* ---------- BUTTON ---------- */
.update-btn{
  grid-column:1/-1;
  margin-top:10px;
  padding:15px;
  border:none;
  border-radius:16px;
  background:linear-gradient(135deg,var(--primary),#4338ca);
  color:white;
  font-size:16px;
  font-weight:700;
  cursor:pointer;
  transition:.3s;
}

.update-btn:hover{
  transform:translateY(-2px);
  box-shadow:0 20px 40px rgba(99,102,241,.4);
}

/* ---------- FOOTER ---------- */
.footer{
  margin-top:30px;
  font-size:13px;
  color:var(--muted);
}

/* ---------- RESPONSIVE ---------- */
@media(max-width:900px){
  .editor{
    grid-template-columns:1fr;
  }
  .preview{
    padding:40px 20px;
  }
}
</style>
</head>

<body>

<div class="editor">

  <!-- BOOK PREVIEW -->
  <div class="preview">
    <img src="<%= book.Image %>" alt="Book Cover">
    <h2><%= book.Booktitle %></h2>
    <p>by <%= book.Authorname %></p>
  </div>

  <!-- FORM -->
  <div class="form-area">
    <h1>Edit Book</h1>
    <span>Update book information carefully</span>

    <form class="form" action="/update-book/<%= book.id %>" method="post">

      <div>
        <label>Book Title</label>
        <input name="Booktitle" value="<%= book.Booktitle %>" required>
      </div>

      <div>
        <label>Author Name</label>
        <input name="Authorname" value="<%= book.Authorname %>" required>
      </div>

      <div class="full">
        <label>Description</label>
        <input name="Desc" value="<%= book.Desc %>" required>
      </div>

      <div class="full">
        <label>Book Image URL</label>
        <input type="url" name="Image" value="<%= book.Image %>">
      </div>

      <div>
        <label>Language</label>
        <select name="Language" required>
          <option value="English" <%= book.Language==="English"?"selected":"" %>>English</option>
          <option value="Hindi" <%= book.Language==="Hindi"?"selected":"" %>>Hindi</option>
          <option value="Gujarati" <%= book.Language==="Gujarati"?"selected":"" %>>Gujarati</option>
        </select>
      </div>

      <div>
        <label>Price (₹)</label>
        <input type="number" name="Price" value="<%= book.Price %>" required>
      </div>

      <button class="update-btn">Save Changes</button>
    </form>

    <div class="footer">
      © 2025 Book Store · Editorial Admin
    </div>
  </div>

</div>

</body>
</html>
